<Page x:Class="ArganaRestaurant.Views.ProductsBoxView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ArganaRestaurant.Views"
      xmlns:components="clr-namespace:ArganaRestaurant.Ressources.Styles.Components" 
      xmlns:VMs="clr-namespace:ArganaRestaurant.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="580"
      Title="ProductsBoxView">
    <Page.DataContext>
        <VMs:ProductsBoxViewModel/>
    </Page.DataContext>

    <Border>
        <Border Margin="5" Padding="0" 
                        CornerRadius="20,20,20,20">
            <Border.Background>
                <SolidColorBrush Color="Transparent"/>
            </Border.Background>


            <ListBox x:Name="ProductsList"
                             Template="{DynamicResource ProductsListBoxTemplate}" 
                             ItemContainerStyle="{DynamicResource ProductsListItemTemplate}"
                             Width="580" Height="500" 
                             Background="Transparent" 
                             BorderThickness="0"
                             ItemsSource="{Binding Products}">

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <components:ProductCard Titel="{Binding Title}" ImageURI="{Binding Image}" Price="{Binding Price}" Totale="{Binding Quantity}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>

                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Width="550" HorizontalAlignment="Center" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>


        </Border>

    </Border>
</Page>
