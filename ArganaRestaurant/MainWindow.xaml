<Window x:Class="ArganaRestaurant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:components="clr-namespace:ArganaRestaurant.Styles.Components"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" Width="1080" Background="Transparent" AllowsTransparency="True"
        WindowStartupLocation="CenterScreen" WindowStyle="None">

    <Window.Resources>
    </Window.Resources>
    <Window.Triggers>
    </Window.Triggers>
    

    <Border x:Name="MainContainer" CornerRadius="10" MouseDown="MainBackground_MouseDown" MouseLeftButtonDown="MainBackground_MouseLeftButtonDown" >
        <Border.Background>
            <!--<RadialGradientBrush>
                <GradientStop Color="White" Offset="0.336"/>
                <GradientStop Color="#FFBDBDBD" Offset="0.858"/>
            </RadialGradientBrush>-->
            <SolidColorBrush Color="#fff" />
        </Border.Background>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#354f52" BorderBrush="Transparent" >
                
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="170"/>
                        <RowDefinition Height="400"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>


                    <Image Grid.Row="0" Source="/Ressources/logo.png" Stretch="Uniform" Height="150" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0 30 0 0"  />

                    <StackPanel Grid.Row="1" Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Button Content="Bestellungen" Style="{StaticResource ProButtonStyle}" Margin="5"/>
                        <Button Content="Bestellungen" Style="{StaticResource ProButtonStyle}" Margin="5"/>
                        <Button Content="Bestellungen" Style="{StaticResource ProButtonStyle}" Margin="5"/>
                        <Button Content="Bestellungen" Style="{StaticResource ProButtonStyle}" Margin="5" />
                        <Button Content="Bestellungen" Style="{StaticResource ProButtonStyle}" Margin="5" />
                    </StackPanel>

                    <Button Grid.Row="2" Content="Ausloggen" Style="{StaticResource ProButtonStyle}" />

                </Grid>
            </Border>

            <Border Grid.Column="1">
                <Border Margin="60" CornerRadius="20,20,20,20">
                    <Border.Background>
                        <SolidColorBrush Color="#FFA0A0A0" Opacity="0.5"/>
                    </Border.Background>


                    <ListBox x:Name="ProductsList" Template="{DynamicResource ProductsListBoxTemplate}" Width="650" Height="540" Background="#FF515050" BorderThickness="0" >
                        <ListBox.Style>
                            <Style/>
                        </ListBox.Style>

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <components:ProductsCard Titel="{Binding Path=Titel}" ImageURI="{Binding Path=ImageURI}" Price="{Binding Path=Price}"  />
                            </DataTemplate>
                        </ListBox.ItemTemplate>

                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel MaxWidth="600" HorizontalAlignment="Center" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Height" Value="240"/>
                                <Setter Property="Width" Value="170"/>
                                <Setter Property="Margin" Value="10 5"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Bd"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                                    Background="{TemplateBinding Background}" 
                                                    Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <MultiTrigger>
                                                    <MultiTrigger.Conditions>
                                                        <Condition Property="IsMouseOver" Value="True"/>
                                                    </MultiTrigger.Conditions>
                                                    <Setter Property="Background" Value="transparent"/>
                                                    <Setter Property="BorderBrush" TargetName="Bd" Value="transparent"/>
                                                </MultiTrigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>

                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>


                </Border>
            </Border>
        </Grid>

    </Border>
</Window>
